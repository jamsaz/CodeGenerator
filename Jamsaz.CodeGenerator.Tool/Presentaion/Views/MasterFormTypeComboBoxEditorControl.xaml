<UserControl x:Class="Jamsaz.CodeGenerator.Tool.Presentaion.Views.MasterFormTypeComboBoxEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Name="root">
    <Grid>
        <!--<telerik:RadComboBox DataContext="{Binding Path=DataContext,ElementName=root}" ItemsSource="{Binding Path=Types}" DisplayMemberPath="Name" />-->
        <telerik:RadComboBox x:Name="ComboBox" DataContext="{Binding Path=DataContext,ElementName=root}" SelectedItem="{Binding Type,Mode=TwoWay}" />
    </Grid>

</UserControl>
